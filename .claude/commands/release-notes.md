---
description: Write the GitHub release notes for a gpui-kit version in the established style
argument-hint: [tag] or the pasted "What's Changed" list
---

Read and follow the shared release-notes workflow in
`.agents/commands/release-notes.md` (relative to the repository root).

Use the following as the workflow input; if empty, use the shared workflow's
default version:

$ARGUMENTS
